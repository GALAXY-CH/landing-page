version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Maintain dependencies for npm (if added in future)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
